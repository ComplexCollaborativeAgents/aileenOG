##!
# @file
#
# @created smohan 20191107

sp {comprehension*apply*parse-content
    (state <s>    ^name comprehension
                  ^operator <op>
                  ^io.output-link <ol>)
    (<op>    ^name parse-content
             ^content <cont>)
-->
    (<ol>    ^language <l>)
    (<l>    ^parse-content <cont>)
    }

sp {comprehension*apply*parse-content*write
    (state <s>    ^name comprehension
                  ^segment <seg>
                  ^operator <op>
                  ^io.input-link.language-link.language.parses <parses>
                  ^superstate.interaction <inter>)
    (<op>    ^name parse-content)
-->
    (<seg>    ^parses <parses>)
    }

sp {comprehension*elaborate*parse*temporal
    (state <s>    ^name comprehension
                  ^segment.parses <parses>)
    (<parses>    ^parse <parse>)
-->
    (<parses>    ^temporal <temp>)
    (<temp>    ^of-parse <parse>
                   ^order <order>)
    }

sp {comprehension*elaborate*parse*temporal*encoding*first-object
    (state <s>    ^name comprehension
                  ^segment.parses <parses>)
    (<parses>    ^parse <parse>
                 ^temporal <temp>)
    (<temp>    ^of-parse <parse>
              ^order <order>)
    (<parse>    ^obj-ref <ref>)
-->
    (<order>    ^first <ref>)
    }

sp {comprehension*elaborate*parse*temporal*encoding*rel-first-object
    (state <s>    ^name comprehension
                  ^segment.parses <parses>)
    (<parses>    ^parse <parse>
                 ^temporal <temp>)
    (<temp>    ^of-parse <parse>
              ^order <order>)
    (<parse>    ^rel-ref.obj-ref <ref>)
-->
    (<order>    ^second <ref>)
    }
                  